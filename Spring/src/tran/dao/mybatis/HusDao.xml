<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tran.dao.HusDao1">
	<insert id="saveHus" parameterType="db.Hus">
		insert into hus values(#{id},#{name},#{age})
	</insert>
</mapper>